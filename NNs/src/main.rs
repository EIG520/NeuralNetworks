extern crate neural_networks;

// use neural_networks::{neural_nets::network::*, model::*};

fn main() {
    // let mut net = TrainableFFNet::new(vec![]);
    //let mut net = TrainableFFNet::trained_on(std::path::Path::new("/home/eig520/Desktop/NeuralNetworks/training_datasets/titanic/train.csv"), vec![1,2,3], vec![4], vec![3,20,1]);

    //println!("{}", net.predict())
}